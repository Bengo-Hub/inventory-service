openapi: 3.0.0
info:
  title: Inventory Service API
  description: HTTP API for the BengoBox inventory service. Provides catalog, warehouse, and inventory management capabilities.
  version: 0.1.0
  contact:
    name: BengoBox Support
servers:
  - url: https://inventory.codevertex.local
    description: Local development server (HTTPS)
  - url: https://inventory.codevrtexitsolutions.com
    description: Production server
paths:
  /healthz:
    get:
      tags:
        - Health
      summary: Service liveness probe
      description: Returns OK when the inventory API process is running.
      responses:
        '200':
          description: Service is alive
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok
                  service:
                    type: string
                    example: inventory-service
  /readyz:
    get:
      tags:
        - Health
      summary: Readiness probe
      description: Validates connectivity to Postgres, Redis, and NATS backends.
      responses:
        '200':
          description: Service is ready
        '503':
          description: Service dependencies unavailable
  /metrics:
    get:
      tags:
        - Health
      summary: Prometheus metrics
      description: Exposes Prometheus metrics for scraping.
      responses:
        '200':
          description: Prometheus metrics payload
  /v1/{tenantID}/inventory/items:
    get:
      tags:
        - Inventory
      summary: List inventory items
      description: Returns a list of inventory items for the specified tenant.
      security:
        - bearerAuth: []
      parameters:
        - name: tenantID
          in: path
          required: true
          schema:
            type: string
          description: Tenant identifier
      responses:
        '200':
          description: List of inventory items
        '401':
          description: Unauthorized
        '500':
          description: Internal server error
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: JWT token from auth-service. Format: Bearer {token}

